#include<stdio.h>
#include<math.h>
int main(){
    int a;
    printf("Enter 1st number : ");
    scanf("%d",&a);
    int b;
    printf("Enter 2nd number : ");
    scanf("%d",&b);

    min(3,8);

    // int q = pow(a,b);
    // printf("%d",q);

    // int root= sqrt(a);
    // printf("The square root is : %d",root);

    return 0;
}